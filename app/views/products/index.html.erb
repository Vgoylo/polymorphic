<h1> Products </h1>

<p><%= link_to "New product", new_product_path(@product)%></p>

<% @products.each do |product| %>
  <%= link_to "#{product.id} Product #{product.name}", product_path(product.id)%>
<%end%>

<h1> Product</h1>

<p> <%= @product.name %></p>
<p> <%= @product.description %></p>

<p>product_id <b><%= @product.id %></b></p>

<td><%= link_to 'Edit', edit_product_path(@product) %></td>
<td><%= link_to 'Destroy', product_path(@product), data: {method: :delete, confirm: 'Are you sure' }%></td>


<p><%= link_to "All products", "/products" %></p>

<div class="container">
  <div class="row", "table table-hover">
    <div class="col-md">
      <%= link_to 'Back', pictures_path, class: 'btn btn-primary' %></td>
      <%= form_for @picture do |f| %>
        <div class="field">
          <%= f.label :nmae %><br/>
          <%= f.text_field :name, required: true, class: "form-control" %>
        </div>
        <div class="actions">
          <%= f.submit 'Save', class:"btn btn-primary" %>
        </div>
      <% end %>
    <div/>
    <div class="col"></div>
  <div/>
<div/>

<div class="container">
  <div class="row">
    <div class="col-md-7">
      <%= link_to 'Back', comments_path, class: 'btn btn-primary' %></td>
      <%= form_for @comment do |f| %>
        <div class="field">
          <%= f.label :text %><br/>
          <%= f.text_field :text, required: true, class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit 'Save', class:"btn btn-primary" %>
        </div>
      <% end %>

    </div>
    <div class="col"></div>
  </div>
</div>